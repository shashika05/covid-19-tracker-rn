import React from "react";
import { View, Text } from "react-native";
import tailwind from "tailwind-rn";

const MoreWorldData = () => {
  return (
    <View style={tailwind("items-center justify-center")}>
      <Text>MoreWorldData</Text>
    </View>
  );
};

export default MoreWorldData;
